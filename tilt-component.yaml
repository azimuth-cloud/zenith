chart: ./charts/server

images:
  zenith-registrar:
    context: ./registrar
    chart_path: registrar.image

  zenith-sshd:
    context: ./sshd
    chart_path: sshd.image

  zenith-sync:
    context: ./sync
    chart_path: sync.image
