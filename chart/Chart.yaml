apiVersion: v2
name: tunnel-proxy-chart
description: Helm chart for deploying a tunnel proxy on Kubernetes
type: application
# The version and appVersion are updated by the chart build script
version: 0.1.0
appVersion: main

# We have a dependency on Consul
dependencies:
  - name: consul
    version: 0.32.1
    repository: https://helm.releases.hashicorp.com
